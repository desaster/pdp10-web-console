# Preview Image

The connect screen for Knight TV shows a grey preview of the terminal. To update it:

1. Connect to the terminal
2. Open browser console (F12)
3. Run `exportFB()`
4. Move downloaded `preview.bin` to `web/public/`
